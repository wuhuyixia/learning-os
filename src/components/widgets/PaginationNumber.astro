---
import type { PaginationNumberProps } from "@interfaces/data";

const {
  number,
  current,
  baseUrl,
  isOnly = false,
} = Astro.props as PaginationNumberProps;
const pageNumber = Number(number);
const href = pageNumber === 1 ? baseUrl : `${baseUrl}/${pageNumber}`;
---

<a
  href={href}
  title={`Page ${number}`}
  aria-label={`Page ${number}`}
  aria-current={pageNumber === current ? "page" : undefined}
  class:list={["join-item btn bg-base-100", pageNumber === current && "btn-active", isOnly && "rounded-btn"]}
>
  {number}
</a>
